package model.user;

/**
 * Класс позволяющий работать с правами администратора
 */
public class Admin extends Client{

    public Admin(String username, String password) {
        super(username, password);
    }
}
